<%= simple_form_for article do |f| %>
  <% if article.new_record? %>
  <% submit = "Create" %>
  <% else %>
  <% submit = "Update" %>
  <% end %>
  <%= f.input :title %>
  <%= f.input :content%>
  <%= f.button :submit, submit, id: "submit-btn"%>
<% end %>
