<h1>Articles</h1>

<% @articles.each do |article|%>
  <div class="article">
    <h2><%= link_to article.title, article_path(article) %></h2>
    <p class="content-preview"><%= article.content.first(500).capitalize... %></p>
  </div>
<% end %>
